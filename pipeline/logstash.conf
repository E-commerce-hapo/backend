input {
	beats {
		port => 5044
	}

	tcp {
		port => 5000
	}
	file {
        path => "/Users/inmac/Downloads/apache-daily-access.log"
        start_position => "beginning" sincedb_path => "/dev/null"
    }
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "changeme"
		ecs_compatibility => disabled
	}
}

setup {
	kibana {
		host => "localhost:5601"
	}
}